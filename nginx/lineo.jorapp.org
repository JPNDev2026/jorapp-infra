server {
    server_name lineo.jorapp.org;
    root /opt/jorapp-infra/pb-mobilier/design;
    index accueil.html;
    location / {
        try_files $uri $uri.html $uri/ =404;
    }
    listen 443 ssl; # managed by Certbot
    ssl_certificate /etc/letsencrypt/live/lineo.jorapp.org/fullchain.pem; # managed by Certbot
    ssl_certificate_key /etc/letsencrypt/live/lineo.jorapp.org/privkey.pem; # managed by Certbot
    include /etc/letsencrypt/options-ssl-nginx.conf; # managed by Certbot
    ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem; # managed by Certbot
}
server {
    if ($host = lineo.jorapp.org) {
        return 301 https://$host$request_uri;
    } # managed by Certbot
    listen 80;
    server_name lineo.jorapp.org;
    return 404; # managed by Certbot
}
